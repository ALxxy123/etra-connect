# 🚀 ETRA Connect

تطبيق تواصل خاص وآمن لفريق ETRA - شات فوري وإدارة ملاحظات مشتركة

![ETRA Connect](https://img.shields.io/badge/ETRA-Connect-blue?style=for-the-badge)
![Next.js](https://img.shields.io/badge/Next.js-14-black?style=flat-square)
![Supabase](https://img.shields.io/badge/Supabase-Realtime-green?style=flat-square)
![PWA](https://img.shields.io/badge/PWA-Ready-purple?style=flat-square)

---

## ✨ المميزات

- 💬 **شات فوري** - رسائل تصل في الوقت الحقيقي
- 📝 **ملاحظات مشتركة** - إضافة مهام لنفسك أو للطرف الآخر
- 🔔 **إشعارات فورية** - تنبيهات عند وصول رسائل جديدة
- 📱 **PWA** - يعمل كتطبيق على الجوال
- 🔒 **آمن** - دخول بالإيميل فقط للمستخدمين المحددين
- 🌙 **تصميم عصري** - واجهة جذابة بتأثيرات Glass

---

## 🛠️ التثبيت

### 1. إعداد Supabase

1. اذهب إلى [supabase.com](https://supabase.com) وأنشئ حساب
2. أنشئ مشروع جديد
3. اذهب إلى **SQL Editor** وشغّل محتوى ملف `supabase-setup.sql`
4. اذهب إلى **Settings > API** وانسخ:
   - Project URL
   - anon public key

### 2. إعداد المشروع

```bash
# استنساخ المشروعب
git clone <your-repo>
cd etra-connect

# تثبيت المكتبات
npm install

# إنشاء ملف البيئةه
cp .env.example .env.local
```

### 3. تعديل ملف البيئة

افتح `.env.local` وأضف مفاتيح Supabase:

```env
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJxxxxxx
```

### 4. تعديل الإيميلات المسموحة

افتح `src/lib/supabase.ts` وعدّل:

```typescript
export const ALLOWED_EMAILS = [
  'ahmad@example.com',  // إيميلك
  'eman@example.com',   // إيميل إيمان
]
```

### 5. تشغيل المشروع

```bash
npm run dev
```

افتح [http://localhost:3000](http://localhost:3000)

---

## 🚀 النشر على Vercel

### الطريقة السهلة

1. ارفع المشروع على GitHub
2. اذهب إلى [vercel.com](https://vercel.com)
3. اربط GitHub واختر المشروع
4. أضف Environment Variables:
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
5. انشر!

### بعد النشر

1. انسخ رابط Vercel (مثلاً: `https://etra-connect.vercel.app`)
2. اذهب إلى Supabase > Authentication > URL Configuration
3. أضف الرابط في:
   - Site URL
   - Redirect URLs

---

## 📱 تثبيت التطبيق (PWA)

### على الجوال (iOS/Android)

1. افتح الموقع في Safari/Chrome
2. اضغط على "مشاركة" أو القائمة
3. اختر "إضافة إلى الشاشة الرئيسية"

### على الكمبيوتر (Chrome)

1. افتح الموقع
2. اضغط على أيقونة التثبيت في شريط العنوان
3. أو: القائمة > تثبيت ETRA Connect

---

## 📁 هيكل المشروع

```
etra-connect/
├── src/
│   ├── app/
│   │   ├── page.tsx          # صفحة تسجيل الدخول
│   │   ├── dashboard/
│   │   │   └── page.tsx      # الصفحة الرئيسية (شات + ملاحظات)
│   │   ├── layout.tsx        # Layout الرئيسي
│   │   └── globals.css       # الأنماط العامة
│   ├── lib/
│   │   ├── supabase.ts       # إعداد Supabase
│   │   └── notifications.ts  # إدارة الإشعارات
│   └── types/
│       └── index.ts          # TypeScript Types
├── public/
│   └── manifest.json         # PWA Manifest
├── supabase-setup.sql        # إعداد قاعدة البيانات
└── README.md
```

---

## 🎨 التخصيص

### تغيير الألوان

افتح `tailwind.config.ts` وعدّل:

```typescript
colors: {
  primary: { ... },  // اللون الرئيسي
  accent: { ... },   // اللون الثانوي
}
```

### إضافة مستخدمين

1. عدّل `ALLOWED_EMAILS` في `src/lib/supabase.ts`
2. عدّل `getUserName()` لإضافة الأسماء

---

## 🔧 الأوامر

```bash
npm run dev      # تشغيل التطوير
npm run build    # بناء للإنتاج
npm run start    # تشغيل الإنتاج
npm run lint     # فحص الكود
```

---

## 📄 الرخصة

هذا المشروع خاص بـ ETRA

---

## 🤝 الدعم

إذا واجهت أي مشكلة، تواصل مع أحمد

---

صنع بـ ❤️ لفريق ETRA
